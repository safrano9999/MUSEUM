schema_version = 1

[[runtime.programs]]
name = "openclaw-gateway"
command = "if command -v openclaw >/dev/null 2>&1; then openclaw gateway start; else echo 'openclaw not installed'; sleep infinity; fi"
directory = "/root/openclaw"
autostart = false
autorestart = true
startsecs = 3
priority = 300
