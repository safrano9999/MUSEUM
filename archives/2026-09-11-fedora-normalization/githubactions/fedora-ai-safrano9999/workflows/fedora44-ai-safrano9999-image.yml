# Source of truth: SCRIPTS/githubactions. Generated copies are overwritten.
name: fedora44-ai-safrano9999 image
run-name: ${{ inputs.monitor_id != '' && format('{0} [monitor:{1}:{2}]', github.workflow, inputs.monitor_id, inputs.monitor_final) || github.workflow }}

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag. Empty uses YYYY.M.D."
        required: false
        default: ""
      base_tag:
        description: "fedora44-ai-base parent tag. Empty uses build.conf"
        required: false
        default: ""
      push_latest:
        description: "Also publish :latest"
        required: true
        type: boolean
        default: true
      monitor_id:
        description: "Optional event-monitor correlation id"
        required: false
        default: ""
        type: string
      monitor_final:
        description: "This run is the final monitored cascade stage"
        required: true
        default: true
        type: boolean

concurrency:
  group: fedora44-ai-safrano9999-image
  cancel-in-progress: false

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      DOCKER_BUILD_RECORD_UPLOAD: "false"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free runner disk space
        run: .github/scripts/free-runner-disk-space.sh

      - name: Resolve image references
        id: refs
        shell: bash
        env:
          REQUESTED_TAG: ${{ inputs.image_tag }}
          BASE_TAG: ${{ inputs.base_tag }}
          PUSH_LATEST: ${{ inputs.push_latest }}
        run: |
          set -euo pipefail
          set -a
          . fedora44-ai-safrano9999/build.conf
          set +a
          version="$REQUESTED_TAG"
          [ -n "$version" ] || version="$(date +%Y.%-m.%-d)"
          base_ref="$AI_BASE_IMAGE"
          [ -z "$BASE_TAG" ] || base_ref="${AI_BASE_IMAGE%:*}:$BASE_TAG"
          image="${FEDORA44_AI_SAFRANO9999_IMAGE%:*}"

          {
            echo "version=$version"
            echo "base_ref=$base_ref"
            echo "image=$image"
            echo "tags<<EOF"
            echo "${image}:${version}"
            [ "$PUSH_LATEST" != true ] || echo "${image}:latest"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Configure source authentication
        env:
          GH_TOKEN: ${{ secrets.GH_RELEASE_TOKEN || github.token }}
        run: gh auth setup-git

      - name: Prepare Safrano build context
        env:
          GH_TOKEN: ${{ secrets.GH_RELEASE_TOKEN || github.token }}
        run: fedora44-ai-safrano9999/build/prepare-build-context.sh --no-cache

      - name: Stage verified KACHELMANN APK
        id: kachelmann_apk
        env:
          GH_TOKEN: ${{ secrets.GH_RELEASE_TOKEN || github.token }}
        run: |
          python3 .github/scripts/stage-kachelmann-apk.py \
            --destination fedora44-ai-safrano9999/safrano9999/KACHELMANN

      - name: Resolve Safrano build configuration
        id: safrano_build
        shell: bash
        run: |
          set -euo pipefail
          resolved=fedora44-ai-safrano9999/.resolved-build.env
          [ -s "$resolved" ] || {
            echo "Missing $resolved" >&2
            exit 1
          }
          {
            echo "build_args<<EOF"
            cat "$resolved"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          cache-binary: false
          keep-state: false
          cleanup: true

      - name: Log in to private GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push Safrano layer
        uses: docker/build-push-action@v6
        with:
          context: ./fedora44-ai-safrano9999
          file: ./fedora44-ai-safrano9999/Containerfile
          target: ai-safrano9999
          platforms: linux/amd64
          pull: true
          no-cache: true
          push: true
          tags: ${{ steps.refs.outputs.tags }}
          labels: |
            io.safrano9999.kachelmann.apk.release=${{ steps.kachelmann_apk.outputs.release_tag }}
            io.safrano9999.kachelmann.apk.filename=${{ steps.kachelmann_apk.outputs.apk_filename }}
            io.safrano9999.kachelmann.apk.sha256=${{ steps.kachelmann_apk.outputs.apk_sha256 }}
          build-args: |
            AI_BASE_IMAGE=${{ steps.refs.outputs.base_ref }}
            ${{ steps.safrano_build.outputs.build_args }}

      - name: Reclaim space after Safrano build
        run: .github/scripts/prune-container-build-space.sh

      - name: Test published Safrano layer
        shell: bash
        run: |
          set -euo pipefail
          image="${{ steps.refs.outputs.image }}:${{ steps.refs.outputs.version }}"
          docker pull "$image"
          docker run --rm --entrypoint bash \
            --env EXPECTED_APK_FILENAME="${{ steps.kachelmann_apk.outputs.apk_filename }}" \
            --env EXPECTED_APK_SHA256="${{ steps.kachelmann_apk.outputs.apk_sha256 }}" \
            "$image" -lc '
            set -euo pipefail
            grep -q "Fedora release 44" /etc/fedora-release
            test -x /usr/local/bin/openclaw-ephemeral.py
            test -x /usr/local/bin/openclaw-ephemeral-yolo
            test -x /usr/local/bin/hermes-ephemeral
            test -x /usr/local/bin/persistainer
            /usr/local/lib/hermes-agent/venv/bin/python -c "import sqlalchemy"
            test -f /root/.openclaw/extensions/note/openclaw.plugin.json
            for project in WELCOME CODEANALYST CITADEL DIESDAS- NEXTCLOUD \
              JUGO VikAI PV_D-A-CH KIWIX_BRIDGE \
              NAPOLEON_HILLS_AI_MASTERMIND_CLASSES \
              SOLANA_AIRGAPPED_DEBIAN_WORKFLOW \
              NaturalGrounding-Tiktok-Ying-Video-Manager \
              DAILYNEWS ZEROINBOX SPANKER KACHELMANN \
              safrano9999-paper; do
              test -d "/opt/safrano9999/$project"
            done
            test -L /README/paper.pdf
            test "$(readlink /README/paper.pdf)" = \
              /opt/safrano9999/safrano9999-paper/paper.pdf
            test -s /README/paper.pdf
            mapfile -t apks < <(find /opt/safrano9999/KACHELMANN -maxdepth 1 \
              -type f -name "kachelmann-20*.apk" -printf "%f\n" | sort)
            test "${#apks[@]}" -eq 1
            test "${apks[0]}" = "$EXPECTED_APK_FILENAME"
            apk="/opt/safrano9999/KACHELMANN/${apks[0]}"
            test "$(sha256sum "$apk" | cut -d " " -f 1)" = "$EXPECTED_APK_SHA256"
            python3 -c "import sys,zipfile; z=zipfile.ZipFile(sys.argv[1]); n=set(z.namelist()); assert {\"AndroidManifest.xml\",\"classes.dex\"} <= n; assert z.testzip() is None" "$apk"
            systemctl is-enabled \
              persistainer.service fedora44-ai-init-hooks.service \
              tailscaled.service tailscale-up.service cloudflared.service \
              cockpit.service vditor-notes.service openclaw-config.service \
              openclaw.service openclaw-ephemeral-schedule.service \
              hermes-ephemeral.service hermes.service hermes-dashboard.service \
              codeanalyst.service citadel.service citadel-scan.service \
              vikai-bootstrap-openclaw-agents.service \
              jugo.service pvdach.service \
              pvdach-qgis-webhook.service kiwix-bridge.service \
              napoleon.service naturalgrounding.service bip39.service \
              spanker-webui.service kachelmann-webui.service \
              kachelmann-mcp.service
            test ! -e /etc/systemd/system/fedora44-ai.service
          '
          test "$(
            docker image inspect \
              --format '{{ index .Config.Labels "io.safrano9999.parent" }}' \
              "$image"
          )" = fedora44-ai-base
          test "$(docker image inspect --format '{{ index .Config.Labels "io.safrano9999.kachelmann.apk.release" }}' "$image")" \
            = "${{ steps.kachelmann_apk.outputs.release_tag }}"
          test "$(docker image inspect --format '{{ index .Config.Labels "io.safrano9999.kachelmann.apk.filename" }}' "$image")" \
            = "${{ steps.kachelmann_apk.outputs.apk_filename }}"
          test "$(docker image inspect --format '{{ index .Config.Labels "io.safrano9999.kachelmann.apk.sha256" }}' "$image")" \
            = "${{ steps.kachelmann_apk.outputs.apk_sha256 }}"
          test "$(docker image inspect --format '{{.Config.User}}' "$image")" = root
          test "$(docker image inspect --format '{{json .Config.Cmd}}' "$image")" \
            = '["/sbin/init"]'
          docker buildx imagetools inspect "$image" >/dev/null
